<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pedrogomez.pokeapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.pedrogomez.pokeapp.PokeListAplication"
        android:theme="@style/Theme.MyListAplication">
        <activity
            android:name="com.pedrogomez.pokeapp.pokedetail.PokemonDetailActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name="com.pedrogomez.pokeapp.pokelist.PokemonsListActivity"
            android:screenOrientation="portrait"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>